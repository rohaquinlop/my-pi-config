[project]
name = "pi-agent-python-tools"
version = "0.1.0"
description = "Python dependencies for local Pi agent tools and extensions"
requires-python = ">=3.12"
dependencies = [
  "certifi>=2025.11.12",
]

[tool.uv]
package = false
